import type { IconProps } from "./types";

export interface ClockIconProps extends IconProps {
  strokeWidth?: number;
}

export function ClockIcon({ size = 20, strokeWidth = 1.8, className }: ClockIconProps) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={strokeWidth}
      strokeLinecap="round"
      strokeLinejoin="round"
      className={className}
      aria-hidden="true"
    >
      <circle cx="12" cy="12" r="9" />
      <path d="M12 7v5l3 3" />
    </svg>
  );
}
